<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/requirement_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/requirement_image_view"
        app:layout_constraintStart_toStartOf="parent"
        android:text="@string/password_requirement_3"
        android:textSize="16sp"
        />

    <ImageView
        android:id="@+id/requirement_image_view"
        android:layout_width="20dp"
        android:layout_height="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/requirement_text_view"
        app:layout_constraintEnd_toEndOf="parent"
        app:tint="@color/success"
        android:src="@drawable/ic_check"
        android:contentDescription="@string/fulfilled_requirement_content_description" />

</androidx.constraintlayout.widget.ConstraintLayout>