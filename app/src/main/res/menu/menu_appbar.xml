<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_search"
        android:title="@string/tab_icon_search_title"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_synchro"
        android:icon="@drawable/ic_action_download"
        android:title="@string/tab_icon_synchro_title"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_online"
        android:icon="@drawable/ic_online_mode"
        android:title="@string/tab_icon_online_mode_title"
        app:showAsAction="ifRoom"/>

</menu>